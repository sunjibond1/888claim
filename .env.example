CLAIM888_HOST=0.0.0.0
CLAIM888_PORT=8123
# Required for Privy wallet login / embedded wallet creation
PRIVY_APP_ID=
# Optional but recommended if your Privy app uses app clients
PRIVY_CLIENT_ID=
# Optional public HTTPS URL for mobile deep links / future staging
PUBLIC_BASE_URL=
